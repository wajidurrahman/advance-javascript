//  const let scope URL:- thatjsdude.com
// step 1

function add(one, two){
    let folafol = one + two;
    return folafol;
}

const view = add(3, 7);
console.log(view);

// step 2
let extra = 30;
function person(rohim, korim) {
    const result = extra + rohim + korim;
    if (result > 10) {
        const vavsab = "valo ace"
        console.log(vavsab);
    }
    return result;
}
const mode = person(5, 7);

// step 3
let bonus = 20;
function sum (first, second){
    let result = first + second + bonus;
    // console.log(bonus);
    if(result > 9){
        var mood = "happy";
            mood = "funky";
            mood = "cranky";
        // console.log(mood);
    }
    return result;
}
const output = sum(3, 7);
console.log(output);

